{
  "scripts": {
    "build": "tsc",
    "start": "nodemon --watch src --ext ts --exec \"npm run start:compiled\"",
    "start:compiled": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "nodemon": "^3.1.11",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/pg": "^8.16.0"
  }
}
